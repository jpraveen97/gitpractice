pipeline {
    agent any

    stages {
        stage('checkout') {
            steps {
                echo 'Cloning code...'
            }
        }

        stage('Builds') {
            steps {
                echo 'Building project...'
            }
        }

        stage('Test') {
            steps {
                echo 'Running tests...'
            }
        }
    }
}
